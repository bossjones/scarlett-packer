#!/bin/bash

set -e
set -x

sudo apt-get clean

sudo sync
sudo sync
sudo sync
sudo sync
sudo sync
sudo sync
sleep 20
sudo sync
sudo sync
sudo sync
sudo sync
sudo sync
sudo sync
sleep 20
sudo sync
sudo sync
sudo sync
sudo sync
sudo sync
sudo sync
sleep 20
sudo sync
sudo sync
sudo sync
sudo sync
sudo sync
sudo sync
sleep 20
